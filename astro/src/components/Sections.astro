---
import Example from "@components/sections/Example.astro";
import Media from "@components/sections/Media.astro";
import ProjectsList from "@components/sections/ProjectsList.svelte";

import type { Sections } from "@/types";

type Props = {
  sections: Sections;
};

const { sections } = Astro.props;
---

{
  sections?.length > 0 &&
    sections.map((section) => {
      switch (section._type) {
        case "section.example":
          return <Example data={section} />;
        case "section.media":
          return <Media data={section} />;
        case "section.projectsList":
          return <ProjectsList client:load data={section} />;
        default:
          return null;
      }
    })
}
